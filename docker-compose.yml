version: "3.7"
services:
  frontend:
    build: ./frontend
  roulette:
    build:
      context: ./backend
      dockerfile: ./roulette/Dockerfile
  proxy:
    build: ./backend/proxy
    ports:
      - "8797:80"
